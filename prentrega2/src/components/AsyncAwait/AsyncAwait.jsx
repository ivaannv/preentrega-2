import React from 'react'

const AsyncAwait = () => {
  return (
    <div>
        <h2>Mis profectos</h2>{
            productos.map((producto, index)=>{
                return(
                    <div>

                    </div>
                )
            })
        }
    </div>
  )
}

export default AsyncAwait